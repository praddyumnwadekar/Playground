#include<iostream>
using namespace std;
int main()
{
  int r,l,d;
  cin>>r>>l;
  d = r * 2;
  if(d == l)
    	cout<<"circle can be inside a square";
  else 
    	cout<<"circle cannot be inside a square";
  //Type your code here.
}